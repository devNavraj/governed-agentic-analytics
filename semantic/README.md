# Semantic layer

YAML metadata that grounds the agent: table/column descriptions, metric definitions,
and allowed joins. Consumed by both the agent (to constrain NL->SQL generation) and as
human-readable docs. Populated in Phase 2 alongside the dbt marts.
