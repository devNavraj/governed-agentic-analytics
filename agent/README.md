# Agent

FastAPI service implementing governed NL analytics: schema-constrained NL->SQL, SQL validation,
BigQuery dry-run cost guard, execution, and grounded answers with row-level citations.
Every call is traced in Langfuse.
