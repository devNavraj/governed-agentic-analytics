# Ingestion

Connector framework for scheduled incremental API ingestion and batch file ingestion
(CSV / Excel / XML), landing raw data in GCS and loading it to BigQuery `raw`.
Designed to be source-agnostic (a Kafka/streaming source can slot in later).
